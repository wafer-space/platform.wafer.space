services:
  docs:
    image: wafer_space_local_docs
    container_name: wafer_space_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./wafer_space:/app/wafer_space:z
    ports:
      - '9000:9000'
    command: /start-docs
