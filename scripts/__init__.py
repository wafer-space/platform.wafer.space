"""Developer and operational scripts for the wafer.space platform.

A regular package (not a namespace package) so tooling resolves modules
consistently as ``scripts.<name>`` — e.g. ``scripts.e2e_verify`` and
``scripts.wait_for_revision``.
"""
